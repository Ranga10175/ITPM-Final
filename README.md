# Hostel Management System

A comprehensive hostel management system designed for SLIIT Campus.

## Project Structure

- **Backend**: Node.js/Express server for API and database management.
- **hostalezone**: Frontend application built with modern web technologies.

## Features

- Student Registration and Authentication
- Booking Management
- Complaint Management UI
- Admin Dashboard
- Notification System

## Getting Started

### Prerequisites
- Node.js
- MongoDB

### Installation
1. Clone the repository
2. Navigate to `Backend` and run `npm install`
3. Navigate to `hostalezone` and run `npm install`

## Development
- Run backend: `cd Backend && npm start`
- Run frontend: `cd hostalezone && npm start`
